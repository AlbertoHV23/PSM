<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.psm.lmaddoubts">
    <!-- Se agrega permisos para acceso a internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".models.sharedPreferences"
        android:allowBackup="true"
        android:icon="@drawable/doubts"
        android:label="@string/app_name"
        android:roundIcon="@drawable/doubts"
        android:supportsRtl="true"
        android:theme="@style/Theme.LMADDOUBTS"
        android:usesCleartextTraffic="true">
        <activity android:name=".RespuestasActivity"></activity>
        <activity android:name=".CreatePostActivity" />
        <activity android:name=".activities.CategoriaActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.LMADDOUBTS.NoActionBar" />
        <activity
            android:name=".activities.SingInActivity"
            android:theme="@style/Theme.LMADDOUBTS.NoActionBar" />
        <activity
            android:name=".activities.LoginActivity"
            android:theme="@style/Theme.LMADDOUBTS.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>